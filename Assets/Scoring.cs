using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[RequireComponent(typeof(NoteOrigin))]
public class Scoring : MonoBehaviour
{
    public int hits;
    public int N; //the total notes

    // Update is called once per frame
    void Update()
    {
        
    }
}
